import { QuizPuzzle } from "@/components/QuizPuzzle";

export const metadata = {
  title: "Daily Quiz — WC26 Hub",
  description: "One World Cup trivia question every day. Test your football knowledge.",
};

export default function QuizPage() {
  return <QuizPuzzle />;
}
